plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace = "app.lawnchair.compatlib.vt"
}

addFrameworkJar('framework-13.jar')

dependencies {
    api projects.compatLib.compatLibVS
}
