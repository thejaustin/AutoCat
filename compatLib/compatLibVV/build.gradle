plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace = "app.lawnchair.compatlib.vv"
}

addFrameworkJar('framework-15.jar')

dependencies {
    api projects.compatLib.compatLibVU
}
