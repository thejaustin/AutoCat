plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace = "app.lawnchair.compatlib.vs"
}

addFrameworkJar('framework-12.jar')

dependencies {
    api projects.compatLib.compatLibVR
}
