name: Crowdin Upload

on:
  push:
    branches:
      - '*-dev'

jobs:

  synchronize-with-crowdin:
    name: Upload strings to Crowdin
    if: github.repository_owner == 'LawnchairLauncher'
    runs-on: ubuntu-latest
    steps:

    - name: Checkout
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v6

    - name: Upload Strings
      uses: crowdin/github-action@02185c3082531050461ad9976e2c87c5d3694982 # v2
      with:
        upload_translations: false
        upload_sources: true
        download_translations: false
        localization_branch_name: 16-dev-localization
        create_pull_request: false
        base_url: 'https://lawnchair.crowdin.com'
      env:
        GITHUB_TOKEN: ${{ secrets.LAWNCHAIR_BOT_TOKEN }}
        CROWDIN_PROJECT_ID: ${{ secrets.CROWDIN_PROJECT_ID }}
        CROWDIN_PERSONAL_TOKEN: ${{ secrets.CROWDIN_PERSONAL_TOKEN }}
