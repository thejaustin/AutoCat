package app.lawnchair.backup.ui

import androidx.compose.runtime.Composable
import androidx.compose.ui.tooling.preview.Preview
import app.lawnchair.backup.ui.CreateBackupViewModel

@Composable
fun CreateBackupScreen(viewModel: CreateBackupViewModel) {
    // TODO: Implement backup creation UI
    // Use the viewModel parameter as needed
}

@Preview
@Composable
private fun CreateBackupScreenPreview() {
    // Preview is disabled since we can't create a proper CreateBackupViewModel without context
    // This is just a placeholder for now
}
