plugins {
    alias(libs.plugins.android.library)
}

android {
    namespace = "com.android.launcher3.androidx"
    sourceSets {
        main {
            java.srcDirs = ['src']
        }
    }
}

addFrameworkJar('framework-16.jar')
compileOnlyCommonJars()
